interface:
  display_name: "Thingsboard"
  short_description: "Repo-specific patterns and workflows for thingsboard"
  default_prompt: "Use the thingsboard repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true